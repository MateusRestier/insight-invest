# API
API_KEY=gere_uma_chave_aleatoria_aqui
# Opcional (serviço único Railway não precisa; usa localhost:$PORT por padrão)
# API_URL=https://insight-invest-api.up.railway.app

# PostgreSQL (Railway / produção)
DB_HOST=seu-host-railway
DB_PORT=5432
DB_NAME=railway
DB_USER=postgres
DB_PASS=sua_senha_aqui

# PostgreSQL local (opcional, apenas com docker compose --profile local up)
# DB_HOST=localhost
# DB_PORT=5432
# DB_NAME=stocks
# DB_USER=user
# DB_PASS=password
